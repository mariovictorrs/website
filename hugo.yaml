baseURL: https://mariovictor.dev/
languageCode: "pt-BR"
title: Mário Victor

module:
  imports:
    - path: github.com/imfing/hextra

outputs:
  home: [HTML,RSS]

menu:
  main:
    - name: Blog
      pageRef: /blog
      weight: 1
    - name: Search
      weight: 2
      params:
        type: search
    - name: GitHub
      weight: 3
      url: "https://github.com/mariovictorrs"
      params:
        icon: github

# Parse Git commit
enableGitInfo: true

params:
  # Display the last modification date
  displayUpdatedDate: true
  dateFormat: "January 2, 2006"

  footer:
    displayCopyright: true
    displayPoweredBy: false
